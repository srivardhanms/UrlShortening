As discussed the logic is in Controller itself and added UT's.
For now the redirection works only for get. 
The baseurl is read from configuration. Set it to localhost and port so that redirection works.